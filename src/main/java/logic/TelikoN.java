package logic;

import org.docx4j.wml.Body;

public class TelikoN {

	public Body parseBody(Body input) {
		// Gets every line
		// Applies transformations to the ones it can
		// When there is doubt, create a comment
		// Return the body, probably for writing
		return null;
	}

	public String parseString(String input) {
		return input;
	}
}
